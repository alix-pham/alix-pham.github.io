+++
author='Alix Pham'
title='How I built this website'
date='2023-02-15'
draft='false'
+++

> This site is built with [Hugo](https://gohugo.io/) and hosted on GitHub Pages. I initially set it up in 2023, and now maintain and update it with the help of AI (Claude Code).

## Setup

**Install dependencies**
* [Git](https://github.com/git-guides/install-git)
* [Hugo](https://gohugo.io/getting-started/installing/)

**Set up a GitHub Pages repo**
* [Follow these instructions](https://pages.github.com/)

**Create the Hugo site and add a theme**
* Run `hugo new site <<your repo name>> --force`
* Download a theme, e.g. [Poison](https://themes.gohugo.io/themes/poison/)
    1. If you won't modify the theme: `git clone <<theme-url>> /themes/<<theme name>>`
    2. If you will make changes: fork the theme repo, then `git submodule add <<forked-theme-url>> themes/<<theme-name>>`

## Add content and test locally

* Edit `config.toml` and `.md` files in the site directory
* Run `hugo server` to preview at http://localhost:1313/
* Run `hugo` to build

## Modify the theme (requires option 2 above)

```
cd themes/<<your theme>>
# make your changes
git add .
git commit -m "<<message>>"
git push origin master
```

If you hit a branch error:
```
git stash
git checkout master
git stash pop
```

## Continuous deployment via GitHub Actions

* [Follow these instructions](https://carpentries-incubator.github.io/blogging-with-hugo-and-github-pages/04-continuous-deployment/index.html)

## Git basics

```
git add .
git commit -m "<<message>>"
git push origin main
```
